# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Editor
.vscode/.ropeproject/
*.code-workspace

# Simulation output (generated by mol_simulation.py, written to output/<molecule>/)
# Position history is one row per atom per recorded tick and can get huge;
# everything else (topology, energy/bond history, plots) is small enough to keep.
output/**/*_positionHistory.csv
*.orig
